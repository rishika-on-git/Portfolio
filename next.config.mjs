/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'api.microlink.io', // Microlink Image Preview
    ],
  },
};

export default nextConfig;
